import { useState } from "react";

function App() {
  const [count, setCount] = useState(0);

  return (
    <div className="App">
      <h2>Observables in hooks testing example</h2>
    </div>
  );
}

export default App;
